 wget https://cli-assets.heroku.com/heroku-linux-x64.tar.gz
  367  unzip heroku-linux-x64.tar.gz
  368  sudo apt install unzip
  369  unzip heroku-linux-x64.tar.gz
  370  ls
  371  tar -xvzf heroku-linux-x64.tar.gz
  372  sudo yum -y install glibc.i686
  373  sudo apt -y install glibc.i686
  374  sudo apt install glibc.i686
  375  sudo apt update
  376  sudo dpkg --add-architecture i386
  377  sudo apt update
  378  sudo apt install libc6:i386
  379  ldd --version
  380  sudo apt -y install glibc.i686
  381  sudo dpkg --add-architecture i386
  382  sudo apt update
  383  sudo apt install libc6:i386
  384  ldd --version
  385  sudo apt -y install glibc.i686
  386  PATH=/heroku/bin/:$PATH
  387  heroku --version
  388  sudo snap install heroku
  389  sudo snap install heroku --classic
  390  pip3 install django
  391  sudo apt install gcc python-setuptools postgresql-devel
  392  sudo apt install python3-setuptools
  393  sudo apt install libpq-dev
  394  sudo apt install gcc
  395  sudo apt install gcc python3-setuptools libpq-dev
  396  ls
  397  heroku login -i
  398  heroku login
  399  export HEROKU_API_KEY=HRKU-9df4d5df-74c8-4898-8d4c-0060b6066f14
  400  heroku login -i
  401  heroku login
  402  unset HEROKU_API_KEY
  403  heroku login
  404  curl https://cli-assets.heroku.com/install.sh | sh
  405  heroku login
  406  sudo snap remove heroku
  407  curl https://cli-assets.heroku.com/install.sh | sh
  408  heroku --version
  409  which heroku
  410  export PATH=/usr/local/bin:$PATH
  411  ls -l /usr/local/bin/heroku
  412  source ~/.bashrc
  413  heroku --version
